# Parrot AI - Voice Cloning Requirements (SaaS Edition)
# Assumes PyTorch with CUDA is already installed

# --- Core TTS ---
qwen-tts>=0.0.5
transformers>=4.45.0
accelerate>=1.0.0
soundfile>=0.13.0
librosa>=0.10.0
numpy>=2.0.0
einops
sox
onnxruntime

# --- Whisper (transcription) ---
openai-whisper>=20231117

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Auth & Supabase ---
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0
gradio_client>=1.3.0

# Flash Attention 2 (requires separate install with --no-build-isolation)
# pip install flash-attn --no-build-isolation
